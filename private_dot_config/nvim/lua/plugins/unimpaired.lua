return {
  'tummetott/unimpaired.nvim',
  event = 'VeryLazy',
  opts = {
    enable_background = false,
    disable_background = false,
    toggle_background = false,
    enable_diff = false,
    disable_diff = false,
    toggle_diff = false,
  },
}
